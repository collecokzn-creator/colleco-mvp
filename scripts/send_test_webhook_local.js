// DEPRECATED: local wrapper used during interactive debugging to send a signed webhook to local server.
// Kept for history. Use `node scripts/send_test_webhook.js` and set `WEBHOOK_URL` / `YOCO_TEST_SECRET` env vars as needed.
console.log('[send_test_webhook_local] DEPRECATED helper - use send_test_webhook.js directly.');
